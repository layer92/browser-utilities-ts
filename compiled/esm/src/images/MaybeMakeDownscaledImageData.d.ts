/** Note: Returns undefined if scaling isn't needed, else returns a scaled copy of the imageData. */
export declare function MaybeMakeDownscaledImageData(imageData: ImageData, maxWidth: number, maxHeight: number): ImageData | undefined;
